# 🤖 AI-Кротик: Telegram Bot с поддержкой ИИ и Поиском

### 📂 Структура проекта
- `bot.js` — **Ядро**. Инициализация Telegraf, входная точка для всех сообщений и координация модулей.
- `modules/` — **Логика**:
  - `config.js` — Конфигурация: токены (.env), ID админа, пути к БД и реестр моделей (OpenRouter/HF).
  - `storage.js` — БД бота. Чтение/запись JSON и **автоматическая очистка истории** (TTL).
  - `menus.js` — **UI/UX**. Описание всех меню, динамические кнопки с ✅ и админ-панель.
  - `handlers.js` — **Контроллер**. Навигация по разделам «Услуги», «Тренинги» и выбор режимов ИИ.
  - `commands.js` — Системные команды: `/start`, `/broadcast` (рассылка), `/logs`.
  - `ai.js` — **Диспетчер ИИ**. Переключает запросы между OpenRouter, Hugging Face и Поиском.
  - `search.js` — **Веб-поиск**. Интеграция с Serper API (Google) для получения свежих данных.
  - `broadcast.js` — Сервис массовой рассылки с поддержкой фото и текста.
  - `roles.js` - Роли для моделей
- `data/` — **Хранилище**:
  - `settings.json` — Профили пользователей (выбранная модель, режим ответа).
  - `history.json` — Контекст диалогов (ограничен по времени и количеству токенов).
  - `broadcasts.json` — История и статистика выполненных рассылок.
  - `funnel_config.json` - База для воронки по сказке
  - `questions.json` - Вопросы от пользователей
 -  `modules/ai-helper.js`: Создан (содержит функцию askAI с перебором моделей)
  - `modules/actions.js`: Создан (содержит все bot.action для заказов, целей Ба Цзы и воронки).
- `modules/funnel.js`: Обновлен (содержит методы init, handleText и processAIQualities с использованием askAI).
- `modules/astro.js`: Обновлен (функция processAI теперь использует askAI вместо старого длинного цикла).


### 🛠 Логика обработки сообщения
1. **Админ-фильтр**: Проверка, не находится ли админ в режиме создания рассылки.
2. **Маршрутизатор**: `handlers.js` проверяет, является ли текст командой меню (Услуги, Режимы и т.д.).
3. **Диспетчер ИИ**:
   - Если модель с префиксом `hf:`, запрос уходит на Hugging Face.
   - Если с префиксом `search:`, сначала вызывается `search.js`, затем результат передается в ИИ.
   - В остальных случаях используется OpenRouter.

### 📦 Основные зависимости
- `telegraf` — Работа с Telegram Bot API.
- `axios` — HTTP-запросы к нейросетям и поисковым сервисам.
- `@huggingface/inference` — Официальный SDK для работы с моделями Hugging Face.
- `dotenv` — Безопасное хранение ключей в файле `.env`
- `node-schedule` - библиотека (планировщик задач) для Node.js, которая позволяет запускать код в строго определенное время или через заданный интервал.

### 🌪 Интерактивная воронка «Личная Сказка» (`modules/funnel.js`)
 модуль для автоматизации продаж через геймификацию и психологические практики.

**Особенности:**
- **Символдрама в PDF**: Автоматическая выдача подарка (мини-практики) в формате документа.
- **МАК-карты (MediaGroup)**: Демонстрация 9 метафорических образов одним альбомом.
- **Интерактивный выбор**: Обработка callback-запросов для каждой карты с персональной интерпретацией.
- **Конверсионные кнопки**: На каждом этапе встроена прямая ссылка на специалиста (`@sherab_wangmo`).


### АстроЧекап (ЛИД-МАГНИТ) `modules/astro.js`
 пользователь получает динамическую интерпретацию карты личности, построенную по китайской системе Ба Цзы через призму своего запроса, с помощью нейросетей
**Особенности:**
- калькулятор Ба Цзы
- передача расчета нейросети с настроенной ролью ответа
- переход к платным консультациям в конце воронки


---
#### 1. Регистрация воронки в ядре (`bot.js`)
Чтобы воронка начала слушать нажатия кнопок, ее нужно инициализировать перед запуском бота:
```javascript
const funnel = require('./modules/funnel');
// ... инициализация других модулей
funnel.init(bot); 

bot.launch();